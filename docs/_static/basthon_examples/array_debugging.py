import numpy as np

# Each of the three lines below contains a mistake.
# Fix them so that the requested arrays are created.

even_numbers = np.arange(0, 10, 2)          # should include 10
many_numbers = np.linspace(0, 10, 100)       # should contain 1000 values
descending_integers = np.arange(100, 1, -1) # should include 1

print("First array:", even_numbers)
print("Second array:", many_numbers)
print("Third array:", descending_integers)
